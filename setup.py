from setuptools import setup, find_packages

setup(
    name="src",  # Name of your package (can be changed)
    version="0.1",
    packages=find_packages(),  # Automatically finds all packages (folders with __init__.py)
)